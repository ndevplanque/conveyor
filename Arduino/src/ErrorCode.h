#ifndef ERROR_CODE_H
#define ERROR_CODE_H

// Définition de l'énumération des codes d'erreurs
enum ErrorCode {
    SUCCESS = 0,
    WIFI_NOT_CONNECTED,
    HTTP_REQUEST_FAILED,
    JSON_PARSING_FAILED
};

#endif
